{
  "name": "shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist && rimraf shared-1.0.0.tgz",
    "build": "yarn clean && tsc && npm pack"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  }
}
